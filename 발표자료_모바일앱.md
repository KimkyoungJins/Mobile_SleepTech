# BLE 수면 데이터 레코더 - 모바일 앱 기술 발표자료

---

## 1. 프로젝트 개요

### 목적
BLE(Bluetooth Low Energy) 센서 디바이스로부터 수면 중 오디오 데이터를 **8시간 이상 연속 수집**하여 서버로 전송하는 Android 모바일 앱

### 기술 스택
| 구분 | 기술 |
|------|------|
| 프레임워크 | React Native 0.81 + Expo 54 |
| BLE 통신 | react-native-ble-plx |
| 백그라운드 실행 | react-native-background-actions (Foreground Service) |
| 파일 시스템 | react-native-fs (RNFS) |
| 언어 | TypeScript |

### 전체 데이터 흐름
```
[BLE 센서] → (Base64) → [앱 메모리 버퍼] → (5초마다) → [로컬 파일 .raw]
                                                              ↓
                                                    (30초 분량 누적 시)
                                                              ↓
                                                       [서버 업로드]
                                                              ↓
                                                       [WAV 변환]
```

---

## 2. Base64 파일 저장 오류 해결

### 2-1. 문제 상황

BLE 디바이스에서 수신하는 데이터는 **Base64 인코딩된 바이너리 오디오 데이터**이다. 초기 구현에서는 수신한 Base64 문자열을 그대로 파일에 이어쓰기(append)했는데, 이 방식에서 **데이터 손상 문제**가 발생했다.

#### 원인: Base64 패딩 충돌
```
// 잘못된 방식: Base64 문자열을 그대로 이어붙이기
"SGVsbG8=" + "V29ybGQ=" → "SGVsbG8=V29ybGQ="  ← 디코딩 시 손상!
```

Base64는 3바이트 단위로 인코딩하며, 나머지가 있으면 `=` 패딩을 추가한다.
여러 Base64 문자열을 그대로 concatenate하면 **중간에 패딩 문자가 섞여** 디코딩 시 데이터가 깨진다.

### 2-2. 해결 방법: 바이너리 레벨 병합

```typescript
// fileStorage.ts - globalFlushBuffer()

// Step 1: 각 Base64 청크를 바이너리(Uint8Array)로 개별 디코딩
const byteArrays = chunksToWrite
  .filter(chunk => chunk)
  .map(chunk => {
    try {
      return toByteArray(chunk);   // base64-js 라이브러리 사용
    } catch (e) {
      console.log('base64 디코딩 에러');
      return null;
    }
  })
  .filter(arr => arr !== null) as Uint8Array[];

// Step 2: 바이너리 배열을 하나로 합치기
const totalLength = byteArrays.reduce((sum, arr) => sum + arr.length, 0);
const combinedArray = new Uint8Array(totalLength);
let offset = 0;
for (const arr of byteArrays) {
  combinedArray.set(arr, offset);
  offset += arr.length;
}

// Step 3: 합쳐진 바이너리를 다시 Base64로 인코딩하여 파일에 저장
const combinedBase64 = fromByteArray(combinedArray);
await RNFS.appendFile(filePath, combinedBase64, 'base64');
```

### 2-3. 핵심 포인트

| 항목 | 잘못된 방식 | 올바른 방식 |
|------|-------------|-------------|
| 처리 단위 | Base64 문자열 연결 | 바이너리(Uint8Array) 병합 |
| 패딩 문제 | 중간 패딩으로 데이터 손상 | 바이너리 병합 후 재인코딩으로 해결 |
| 라이브러리 | 기본 atob/btoa | base64-js (toByteArray/fromByteArray) |
| 파일 쓰기 | 문자열 모드 | `'base64'` 인코딩 모드로 RNFS 저장 |

### 2-4. 에러 처리 및 데이터 복구

파일 쓰기 실패 시 **데이터 유실을 방지**하는 로직도 추가했다:

```typescript
try {
  // ... 파일 쓰기 시도
} catch (err) {
  // 실패 시 버퍼에 데이터 복구 → 다음 flush에서 재시도
  globalWriteBuffer = [...chunksToWrite, ...globalWriteBuffer];
}
```

---

## 3. 백그라운드 동작 안정화

수면 데이터 수집 앱의 특성상 **8시간 이상 백그라운드에서 안정적으로 동작**해야 한다. Android에서 백그라운드 프로세스를 유지하기 위해 여러 기술적 문제를 해결했다.

### 3-1. Android Foreground Service 활용

Android는 백그라운드 앱을 적극적으로 종료한다. 이를 방지하기 위해 **Foreground Service**를 사용했다.

```typescript
// backgroundService.ts
export const startBackgroundService = async () => {
  await BackgroundService.start(backgroundTask, {
    taskName: 'SleepStudyRecorder',
    taskTitle: '수면 데이터 기록 중',        // 알림바 표시
    taskDesc: '백그라운드에서 센서 데이터를 수집하고 있습니다.',
    linkingURI: 'blereact:///',              // 알림 터치 시 앱으로 복귀
    parameters: { delay: 5000 },
  });
};
```

### 3-2. Android 10+ Foreground Service 타입 패치

Android 10(API 29) 이상에서는 Foreground Service 시작 시 **서비스 타입을 명시**해야 한다. `react-native-background-actions` 라이브러리가 이를 지원하지 않아 **직접 패치**를 적용했다.

```java
// patches/react-native-background-actions+4.0.1.patch
// Android 10+ (API 29) requires foregroundServiceType parameter
if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.Q) {
    startForeground(
        SERVICE_NOTIFICATION_ID,
        notification,
        ServiceInfo.FOREGROUND_SERVICE_TYPE_DATA_SYNC
          | ServiceInfo.FOREGROUND_SERVICE_TYPE_CONNECTED_DEVICE
    );
} else {
    startForeground(SERVICE_NOTIFICATION_ID, notification);
}
```

- `DATA_SYNC`: 데이터 동기화(파일 저장 및 서버 업로드)
- `CONNECTED_DEVICE`: BLE 연결 디바이스 통신

### 3-3. HeadlessJS와 전역 변수 패턴

React Native의 백그라운드 서비스는 **HeadlessJS**로 실행된다. HeadlessJS는 React 컴포넌트 트리 외부에서 동작하므로, `useState`나 `useRef`로 관리하는 상태에 접근할 수 없다.

이 문제를 해결하기 위해 **전역 변수 패턴**을 사용했다:

```typescript
// fileStorage.ts - 전역 변수로 상태 관리
export let globalWriteBuffer: string[] = [];   // 쓰기 버퍼
export let globalIsRecording = false;          // 녹음 상태
export let globalFileInitialized = false;      // 파일 초기화 여부

// React 컴포넌트에서 전역 상태 동기화
useEffect(() => {
  setGlobalIsRecording(isRecording);  // React state → 전역 변수
}, [isRecording]);
```

```
[React 컴포넌트 영역]          [HeadlessJS 영역]
  useState(isRecording)   →   globalIsRecording (전역 변수)
  useRef(writeBuffer)     →   globalWriteBuffer (전역 변수)
                                    ↓
                              backgroundTask()에서 직접 접근
```

### 3-4. BLE 자동 재연결

수면 중 BLE 연결이 끊어질 수 있다. 녹음 중에는 **지수 백오프(Exponential Backoff)** 전략으로 최대 10분간 자동 재연결을 시도한다:

```typescript
// useBLE.ts - attemptReconnect()
const backoff = Math.min(5000 * reconnectAttemptRef.current, 30000);
// 1회차: 5초 후 재시도
// 2회차: 10초 후 재시도
// 3회차: 15초 후 재시도
// ...최대 30초 간격
// 총 10분간 반복
```

### 3-5. 배터리 최적화 제외 안내

Samsung 등 Android 제조사는 자체 배터리 최적화로 백그라운드 앱을 종료한다.
앱 시작 시 사용자에게 **배터리 최적화 제외 설정**을 안내한다:

```typescript
Alert.alert(
  '배터리 최적화 제외 필요',
  '앱이 백그라운드에서 8시간 이상 안정적으로 동작하려면 배터리 최적화에서 제외해야 합니다.',
  [
    { text: '나중에', style: 'cancel' },
    { text: '설정으로 이동', onPress: () => Linking.openSettings() },
  ]
);
```

### 3-6. 화면 꺼짐 방지

`expo-keep-awake`를 사용하여 녹음 중 화면이 꺼지지 않도록 설정:

```typescript
// 녹음 시작 시
await activateKeepAwakeAsync('recording');

// 녹음 종료 시
deactivateKeepAwake('recording');
```

---

## 4. 메모리 사용량 최적화

8시간 연속 녹음 시 메모리 누수나 과다 사용은 앱 강제 종료로 이어진다. 다음과 같은 최적화를 적용했다.

### 4-1. 버퍼 크기 제한 (OOM 방지)

파일 쓰기(flush)가 실패하면 버퍼가 무한히 커질 수 있다. 이를 방지하기 위해 **최대 버퍼 크기를 제한**했다:

```typescript
// backgroundService.ts
const MAX_BUFFER_SIZE = 2000;  // 약 60초 분량

if (globalWriteBuffer.length > MAX_BUFFER_SIZE) {
  const dropped = globalWriteBuffer.length - MAX_BUFFER_SIZE;
  globalWriteBuffer.splice(0, dropped);  // 오래된 데이터부터 제거
  console.log(`버퍼 초과: ${dropped}개 청크 제거`);
}
```

**설계 의도**: flush 실패가 반복되더라도 메모리 사용량이 일정 수준(약 60초 분량) 이상으로 증가하지 않도록 보장한다. 오래된 데이터를 먼저 버리는 전략으로, 최신 데이터를 최대한 보존한다.

### 4-2. 5초 주기 배치 쓰기 (Batch Write)

BLE 패킷이 들어올 때마다 파일에 쓰는 대신, **버퍼에 모았다가 5초마다 한 번에 파일에 기록**한다:

```
매 패킷마다 쓰기 (X):     write → write → write → write → ...
5초 배치 쓰기 (O):        [buffer] [buffer] [buffer] → flush → [buffer] ...
```

| 항목 | 매번 쓰기 | 5초 배치 쓰기 |
|------|-----------|---------------|
| I/O 횟수 | 초당 수십~수백 회 | 5초당 1회 |
| CPU 사용 | 높음 | 낮음 |
| 메모리 | 매번 인코딩 오버헤드 | 버퍼 크기만큼만 사용 |
| 배터리 | 빠른 소모 | 효율적 |

### 4-3. UI 업데이트 쓰로틀링 (10초 간격)

BLE 패킷이 초당 수십~수백 개 들어오는데, 매번 React 상태를 업데이트하면 **과도한 리렌더링**으로 메모리와 CPU를 낭비한다.

```typescript
// useBLE.ts - 데이터 수신 콜백 내부
const now = Date.now();
if (now - timeRef.current > 10000) {   // 10초에 한 번만 UI 업데이트
  setPacketCount(packetCountRef.current);
  setLogs((prev) => [logEntry, ...prev].slice(0, 50));
  timeRef.current = now;
}
```

### 4-4. 백그라운드 시 UI 업데이트 완전 차단

앱이 백그라운드에 있을 때는 UI가 보이지 않으므로 **상태 업데이트를 아예 하지 않는다**:

```typescript
// useBLE.ts - 데이터 수신 콜백
if (!isAppActiveRef.current) {
  return;  // 백그라운드면 UI 업데이트 스킵 → setState 호출 안 함
}
```

이를 통해 백그라운드에서는 **데이터 수신 → 버퍼 추가**만 수행하고, React 리렌더링은 발생하지 않는다.

### 4-5. 로그 배열 크기 제한

UI 로그와 업로드 로그 모두 **최대 50개**로 제한하여 메모리 누적을 방지:

```typescript
// UI 로그
setLogs((prev) => [logEntry, ...prev].slice(0, 50));

// 업로드 로그
globalUploadLogs.unshift({ timestamp, text, success });
if (globalUploadLogs.length > 50) {
  globalUploadLogs = globalUploadLogs.slice(0, 50);
}
```

### 4-6. 버퍼 스냅샷 후 즉시 비우기 (Copy-and-Clear)

```typescript
// fileStorage.ts - globalFlushBuffer()
const chunksToWrite = [...globalWriteBuffer];  // 복사
globalWriteBuffer = [];                         // 즉시 비움 → 새 데이터 수신 가능
```

버퍼를 복사한 뒤 즉시 비우므로, 파일 쓰기 중에도 새로운 BLE 데이터를 계속 받을 수 있고, 메모리에 데이터가 이중으로 쌓이는 시간을 최소화한다.

---

## 5. 메모리 최적화 효과 요약

```
┌────────────────────────────────────────────┐
│          메모리 사용 패턴 (8시간)            │
│                                            │
│  최적화 전:  ──────────────/  (계속 증가)   │
│  최적화 후:  ~~~~~~~~~~~~~~~~~~~~~~~~       │
│              (일정하게 유지)                  │
└────────────────────────────────────────────┘
```

| 최적화 기법 | 효과 |
|-------------|------|
| 버퍼 크기 제한 (2000개) | OOM 크래시 방지 |
| 5초 배치 쓰기 | I/O 횟수 감소, CPU/배터리 절약 |
| UI 업데이트 10초 쓰로틀링 | 불필요한 리렌더링 제거 |
| 백그라운드 시 UI 차단 | setState 호출 완전 제거 |
| 로그 50개 제한 | 로그 메모리 누적 방지 |
| Copy-and-Clear 패턴 | 버퍼 이중 점유 최소화 |

---

## 6. 서버 업로드 아키텍처

### 30초 청크 업로드

전체 녹음 파일을 한 번에 보내지 않고, **30초 분량(960KB)이 모일 때마다** 서버로 전송:

```typescript
const CHUNK_SIZE_30SEC = 960000; // 16kHz × 16bit × 1ch × 30초 = 960,000 bytes

// 새 데이터가 30초 분량 이상이면 업로드
if (newDataSize >= CHUNK_SIZE_30SEC) {
  await uploadChunk();
}
```

### Offset 기반 증분 읽기

```typescript
// 마지막으로 보낸 위치부터 새 데이터만 읽기
const newDataBase64 = await RNFS.read(filePath, newDataLength, lastOffset, 'base64');
// 성공 시 offset 업데이트
updateLastSentOffset(fileSize);
```

전체 파일을 메모리에 올리지 않고 **필요한 부분만 읽어서** 메모리를 절약한다.

---

## 7. 정리

| 핵심 과제 | 해결 방법 |
|-----------|-----------|
| Base64 파일 손상 | 바이너리 레벨 병합 후 재인코딩 |
| 백그라운드 종료 | Foreground Service + 배터리 최적화 제외 |
| Android 10+ 호환 | foregroundServiceType 패치 |
| HeadlessJS 상태 접근 | 전역 변수 패턴 |
| BLE 연결 끊김 | 지수 백오프 자동 재연결 (최대 10분) |
| 메모리 과다 사용 | 버퍼 제한 + 배치 쓰기 + UI 쓰로틀링 |
| 네트워크 효율 | 30초 청크 단위 증분 업로드 |
