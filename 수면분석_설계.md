# 수면 분석 기능 설계

---

## 1. 서버 → 모바일 API 응답 구조

서버에서 딥러닝 분석 결과를 모바일로 내려주는 데이터 형식이다.

### API 엔드포인트

```
GET /api/result/{session_id}
```

### 응답 JSON

```json
{
  "session_id": "20260312_231500",

  "total_sleep_minutes": 420,
  "sleep_start": "2026-03-12T23:15",
  "sleep_end": "2026-03-13T07:15",

  "stages": {
    "nrem_minutes": 270,
    "rem_minutes": 105,
    "wake_minutes": 45
  },

  "timeline": [
    { "time": "23:15", "stage": "NREM" },
    { "time": "23:30", "stage": "NREM" },
    { "time": "23:45", "stage": "NREM" },
    { "time": "00:00", "stage": "REM" },
    { "time": "00:15", "stage": "WAKE" }
  ]
}
```

### 각 필드 설명

| 필드 | 설명 | 단위 |
|------|------|------|
| `session_id` | 녹음 세션 식별자 (YYYYMMDD_HHmmss) | - |
| `total_sleep_minutes` | 전체 수면 시간 | 분 |
| `sleep_start` | 취침 시각 | ISO 8601 |
| `sleep_end` | 기상 시각 | ISO 8601 |
| `stages.nrem_minutes` | NREM 수면 시간 | 분 |
| `stages.rem_minutes` | REM 수면 시간 | 분 |
| `stages.wake_minutes` | 중간 각성 시간 | 분 |
| `timeline` | 15분 간격 수면 단계 배열 (하이프노그램용) | - |

> `timeline`은 하이프노그램(수면 그래프)을 그리기 위해 반드시 필요하다. 딥러닝 분류 결과를 시간대별로 서버에서 내려줘야 한다.

---

## 2. 모바일에서 연산: 수면 품질 점수

서버에서 받은 단계별 시간 데이터를 기반으로 **모바일에서 점수를 계산**한다.

### 점수 계산 공식 (100점 만점)

```typescript
const calculateScore = (stages, totalMinutes) => {
  const nremRatio = stages.nrem_minutes / totalMinutes;
  const remRatio = stages.rem_minutes / totalMinutes;
  const wakeRatio = stages.wake_minutes / totalMinutes;

  let score = 50; // 기본 점수

  // NREM 수면 비율 (이상적: 70~80%)
  if (nremRatio >= 0.70 && nremRatio <= 0.80) score += 20;
  else if (nremRatio >= 0.60) score += 10;

  // REM 수면 비율 (이상적: 20~25%)
  if (remRatio >= 0.20 && remRatio <= 0.25) score += 20;
  else if (remRatio >= 0.15) score += 10;

  // 총 수면시간 보너스 (7~9시간이 이상적)
  if (totalMinutes >= 420 && totalMinutes <= 540) score += 10;

  // 중간 각성 패널티
  score -= Math.round(wakeRatio * 40);

  return Math.max(0, Math.min(100, score));
};
```

### 점수 기준 근거

| 항목 | 이상적 범위 | 배점 | 근거 |
|------|-------------|------|------|
| NREM 수면 비율 | 70~80% | +20점 | 신체 회복, 성장호르몬 분비 구간 (N1+N2+N3) |
| REM 수면 비율 | 20~25% | +20점 | 기억 강화, 감정 처리 구간 |
| 총 수면 시간 | 7~9시간 | +10점 | 성인 권장 수면시간 (NSF 기준) |
| 중간 각성 | 낮을수록 좋음 | -최대 40점 | 수면 연속성 지표 |

---

## 3. 시각화 설계

### 3-1. 수면 점수 카드

점수 구간에 따라 **색상 + 문구**를 동적으로 변경한다.

| 점수 | 등급 | 색상 (HEX) | 문구 |
|------|------|------------|------|
| 85~100 | 매우 좋음 | `#7EE787` (초록) | "푹 잤어요!" |
| 70~84 | 좋음 | `#58A6FF` (파랑) | "양호한 수면이에요" |
| 50~69 | 보통 | `#D29922` (노랑) | "조금 아쉬운 수면이에요" |
| 0~49 | 나쁨 | `#FF6B6B` (빨강) | "수면 개선이 필요해요" |

**UI 구성**: 원형 점수 표시 + 등급 문구 + 총 수면시간

### 3-2. 수면 단계 비율 바

가로 막대에 각 수면 단계를 비율대로 표시한다.

```
[██████████████ NREM ████████ REM ██ 각성]
       64%              25%      11%
```

| 단계 | 색상 | 의미 |
|------|------|------|
| NREM | `#8B5CF6` (보라) | 비렘 수면 (N1+N2+N3) |
| REM | `#58A6FF` (파랑) | 렘 수면, 기억 강화 |
| WAKE | `#FF6B6B` (빨강) | 중간에 깬 시간 |

### 3-3. 하이프노그램 (수면 타임라인)

서버에서 받은 `timeline` 배열을 기반으로 시간순 수면 단계 그래프를 그린다.

```
WAKE  ┃█                              █
REM   ┃   ████          ████     ████
NREM  ┃██      ████████      ████
      ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
       23:00  00:00  01:00  02:00  03:00
```

- X축: 시간 (15분 간격)
- Y축: 수면 단계 (NREM → WAKE, 아래에서 위로)
- 각 막대 색상: 수면 단계별 지정 색상

---

## 4. 역할 분담 요약

```
┌─────────────────────┐          ┌─────────────────────┐
│     서버 (딥러닝)      │          │     모바일 (표시)      │
├─────────────────────┤          ├─────────────────────┤
│ 수면 단계 분류         │  ──→    │ 타임라인 그래프 렌더링   │
│ 단계별 시간 집계       │  ──→    │ 비율 바 렌더링         │
│ 총 수면시간 계산       │  ──→    │ 수면 점수 계산 (공식)   │
│ 타임라인 생성 (15분)   │  ──→    │ 점수별 색상/문구 매핑   │
│                      │          │ 이력 저장 (AsyncStorage)│
└─────────────────────┘          └─────────────────────┘
```

### 서버가 보내는 것
- 전체 수면시간
- 각 수면 단계별 시간 (nrem, rem, wake)
- 15분 간격 타임라인 배열

### 모바일이 연산하는 것
- 수면 품질 점수 (단계 비율 기반 공식)
- 점수별 등급/색상/문구 매핑
- 그래프 렌더링 (비율 바, 하이프노그램)

### 모바일이 연산하는 이유
점수 공식을 모바일에 두면 **서버 수정 없이 점수 기준을 조정**할 수 있고, 서버는 객관적 데이터만 제공하여 역할이 명확해진다.

---

## 5. 추후 확장 가능

| 기능 | 설명 |
|------|------|
| 주간 평균 비교 | "지난 7일 평균보다 12분 더 잤어요" |
| 수면 단계 전환 횟수 | 뒤척임 지표 (timeline에서 stage 변경 횟수 카운트) |
| 수면 이력 캘린더 | AsyncStorage에 날짜별 결과 저장 후 캘린더 뷰 |
| 수면 팁 제공 | 점수 기반 맞춤 수면 개선 조언 |
